/**
 * Placeholder. In Fase 7 (Supabase + database) questo file verrà sostituito
 * dall'output reale di:
 *   supabase gen types typescript --project-id <id> > lib/supabase/types.ts
 *
 * Non modificare manualmente una volta generato: rigenerare dopo ogni
 * migrazione dello schema.
 */
export type Database = Record<string, unknown>;
